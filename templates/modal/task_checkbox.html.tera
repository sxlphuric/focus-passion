<input type=checkbox id="modal-completed-state-{{task.id}}"
        hx-post="/tasks/complete/{{task.id}}"
        hx-trigger="click"
        hx-swap="outerHTML"
        hx-target="#task-{{task.id}}"
        autocomplete="off"
        {% if task.completed %}checked{%endif%}>
<svg>
    <use xlink:href="#checkbox-30" class="checkbox"></use>
</svg>
